# 面向 JVM 差分测试的“5维感知变异”增强型 Fuzzing 方案说明

这份文档旨在为你的方案介绍 PPT 提供核心素材，同时详细展示本项目如何通过“五维全息分类漏斗”和“定制化变异机制”解决 LLM 生成代码过于平凡的问题，并明确展示其中的技术创新与工程工作量。

---

## 1. 方案定位：不仅仅是 LLM 生成
**核心痛点**：单纯依靠 LLM（Qwen/DeepSeek）生成的测试代码往往遵循“常规用法”，难以触及 JVM 内部复杂的边界逻辑（如 buffer 溢出、资源竞争、状态机异常）。
**本方案方案**：构建一套以**文档学习为导向、反馈优化的提示词（Prompt）**为骨架，通过**分类感知的硬核变异（Category-Aware Mutation）**进行精准边界打击的混合型 Fuzzing 系统。

---

## 2. 总体架构：协同进化闭环

系统由四个核心逻辑层组成，形成一个持续演化的闭环：

1.  **知识蒸馏层 (Knowledge Distillation)**:
    - **输入**: API 官方 Markdown 文档。
    - **逻辑**: 使用 DeepSeek 对冗长文档进行“语义压缩”，提取方法签名、异常说明和边界条件。
    - **产出**: 结构化的 API 行为概要，作为后续代码生成的种子提示词（Prompt）。

2.  **5维全息分类漏斗 (5D Holographic Funnel)**:
    - **原理**: 对 API 进行多维度（Package, Type, Interface, Field, Method + Boundary）扫描。
    - **逻辑**: 将 API 映射到特定的**变异画像 (Mutation Profile)**。例如 `BufferedOutputStream` 被识别为 `RESOURCE + BUFFER` 类型。
    - **产出**: 分类标签，直接决定后续变异算子的选择。

3.  **混合变异引擎 (Hybrid Mutation Engine)**:
    - **模型驱动变异**: 指导 LLM 围绕“资源生命周期”或“参数组合”进行逻辑重写。
    - **规则驱动变异 (Deterministic)**: **(关键工作量)** 基于正则/AST 的硬核算子（如构造器 size 边界注入、off/len 溢出计算、close/flush 乱序重排）。
    - **产出**: 大量具有高撞击能量的测试样例文件（`.fuzz`）。

4.  **反馈驱动进化 (Feedback-Driven Evolution)**:
    - **执行**: 在目标 JVM/编译器上运行生成的样例，获取 `.log` 信息。
    - **逻辑**: 将编译错误（Cannot find symbol, IOException 等）结构化为“规则”，由 DeepSeek 重新优化 Prompt，下一轮生成的 seed 将天然避开低级语法错误，直击深层逻辑风险。

---

## 3. 5维全息分类漏斗 (创新项)

我们将 API 置于五个维度下观察，以决定变异的方向：

| 维度 | 观察重点 | 变异导向示例 |
| :--- | :--- | :--- |
| **Package** | 领域位置 (`.io`, `.net`) | 确定是偏文件系统还是网络连接。 |
| **Type** | 类名语义 (`Stream`, `Lock`) | 确定是流式操作还是并发原语。 |
| **Interface** | 接口约定 (`Flushable`, `Closeable`) | 驱动“早关闭”、“重复刷新”等生命周期变异。 |
| **Field** | 内部字段 (`buf`, `count`, `pos`) | 识别缓冲区结构，驱动“容量边界”变异。 |
| **Method** | 方法特征 (`write(byte[], int, int)`) | 驱动“偏移量/长度”溢出变异。 |
| **Boundary** | 异常说明 (`NullPointerException`) | 确定高危异常路径，驱动极端值注入。 |

---

## 4. 变异画像与算子自定义 (工作量体现)

针对不同的分类，我们定制了差异化的变异算子 (Operators)：

### A. RESOURCE + BUFFER 画像 (如 BufferedOutputStream)
*   **算子1: `constructor_size_edge`**: 将构造函数中的 buffer size 强制修改为 `1`, `0`, `-1`, 或 `Integer.MAX_VALUE`。
*   **算子2: `write_bulk_overflow`**: 对 `write(buf, off, len)` 里的 `off+len` 进行大于 `buf.length` 的算术溢出变异。
*   **算子3: `lifecycle_interleave`**: 在 `close()` 之后尝试 `write()` 或重复 `flush()`，探测状态机漏洞。

### B. CONCURRENT 画像 (如 Thread, Lock)
*   **算子1: `race_condition_generator`**: 引入多线程交错调用同一 API 实例。
*   **算子2: `deadlock_template`**: 构造嵌套锁或循环等待代码结构。

### C. FILE 画像 (如 Path, Files)
*   **算子1: `path_traversal_mutation`**: 注入 `../`, `\0`, 极大长度路径等非法字符。
*   **算子2: `state_transition_mutation`**: 在未创建文件前调用 read/write，或在删除后调用。

---

## 5. 方案创新性与工程价值

1.  **可解释的变异 (Explainable Mutation)**:
    - 区别于盲目的随机变异，每一个变异动作都有“分类画像”支持，生成的样例更符合 Java 开发者的真实场景和 JVM 的实现盲区。
2.  **双层筛选机制 (Repair & Filter)**:
    - 在变异前后引入 `Repair` (自动纠正 byte 越界、补齐 import) 和 `Filter` (丢弃幻觉 API)。这保证了生成的样例“外表稳健、内核尖锐”。
3.  **可追踪的证据链 (Mutation Manifest)**:
    - 系统自动生成 `mutation_manifest.jsonl`，记录每个样例的：父种子、所用算子、分类标签、编译结果。这不仅是实验分析的依据，也是你工作量的直接证明。

---

## 6. 后续实验与改进计划 (Next Steps)

1.  **全量 API 扫描**: 扩展 `scripts/classify_java_apis.py`，完成 Java 全量核心 API 的画像描绘。
2.  **差分测试接入**: 将生成的 `.fuzz` 样例自动投递到 OpenJDK 8, 11, 17, 21 等不同版本进行分歧检测。
3.  **性能收敛**: 引入更小的验证批次（Validation Batch），削减模型交互成本，提升生成效率。

---

## 适合 PPT 讲稿的核心结论

> “本工作不仅利用了 LLM 的生成能力，更通过 **5D 全息分类漏斗** 对 Java API 建立了深度风险感知。我们设计了一套**分类驱动的定制化变异引擎**，将普通的语义骨架推进到极具挑战性的边界区域，为 JVM 的差分测试提供了高质量、多维度的压力源。这套系统在修复幻觉、定位风险、以及自动化迭代方面，相比于原生 LLM 生成具有显著的优越性。”
