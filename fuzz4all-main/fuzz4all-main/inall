# 面向 JVM 差分测试的“文档学习-提示词生成-代码生成-运行反馈-分类感知变异-再次优化”完整方案说明

## 0. 文档用途与阅读方式

这份文档不是简短汇报稿，而是给三类对象共同使用的“工程说明书”：

1. 你自己拿来做方案介绍 PPT。
2. 其他 AI 拿到后，能快速理解你项目的真实工作流和每一层为什么这样设计。
3. 后续继续迭代代码时，作为统一设计基线。

这份文档重点回答 8 个问题：

1. 你现在的系统到底在做什么。
2. 从 DeepSeek 到 Qwen 再到编译反馈，完整链路是什么。
3. 为什么要引入“五维全息分类漏斗 + api_classify”。
4. 分类到底怎么分，分这些类的理由是什么。
5. mutation 为什么必须做，应该做在哪里。
6. 每类 API 应该做什么样的变异，为什么这样做。
7. 代码里具体应该改哪些文件、哪个环节插进去最合适。
8. 这套方案的创新点、工作量和后续实验该怎么讲。

---

## 0.1 术语表：文中常见学术词和英文的白话解释

为了保证别的 AI、老师或者不熟悉 fuzzing 的读者也能看懂，下面先把文中会频繁出现的词解释清楚。

### `API`

全称：

- `Application Programming Interface`

白话解释：

- 这里可以理解为“一个类、接口或者库对外提供的可调用能力”
- 比如 `BufferedOutputStream.write(...)`、`flush()` 都属于 API 行为

### `prompt`

白话解释：

- 给大模型的“任务说明书”或“提示词”
- 它告诉模型应该往哪个方向写代码、避开什么错误、关注哪些边界

### `auto-prompting`

白话解释：

- 不是人工手写 prompt，而是先让另一个模型自动帮我们生成 prompt
- 在本项目里，DeepSeek 先看文档，再帮 Qwen 生成更适合写代码的提示词

### `candidate`

白话解释：

- 候选项
- 例如 DeepSeek 一次生成多个候选 prompt，再从里面挑最好的

### `greedy prompt`

白话解释：

- 倾向于“更保守、更确定”生成方式得到的 prompt
- 可以理解成“默认主候选”

### `seed`

白话解释：

- 种子样例
- 指模型最初生成出来的基础代码，它后面会被拿去做 mutation

### `mutation`

中文：

- 变异

白话解释：

- 对已有代码做定向改动，得到新的变体
- 不是重新从零写，而是在已有样例上改参数、改顺序、改结构

### `mutation profile`

中文：

- 变异画像 / 变异配置档案

白话解释：

- 某类 API 适合使用什么变异策略的说明书
- 比如 `BufferedOutputStream` 适合做 buffer 大小、write 边界、flush/close 顺序变异

### `operator`

中文：

- 算子 / 变异操作器

白话解释：

- 一种具体变异动作
- 例如 `constructor_size_edge` 就是“改构造器 size 参数”的算子

### `feedback`

中文：

- 反馈

白话解释：

- 运行编译器或程序后得到的结果信息
- 在这里主要是编译日志和错误信息

### `runtime feedback`

白话解释：

- 运行中的反馈
- 指系统在一轮样例生成结束后，把本轮结果重新总结给下一轮 prompt 使用

### `SAFE`

白话解释：

- 当前样例至少通过了当前编译验证
- 不是“绝对没有问题”，而是“在当前 oracle 下没有报编译错误”

### `FAILURE`

白话解释：

- 当前样例在验证阶段失败
- 一般是编译不通过或出现明确错误

### `oracle`

中文：

- 判定器 / 评价准则

白话解释：

- 系统用来判断样例“是否合格”的规则
- 在当前阶段，最主要的 oracle 是 Java 编译器结果

### `profile-aware`

白话解释：

- 感知画像的
- 指系统会先知道当前 API 属于哪一类，再决定采用哪一类变异

### `JVM differential testing`

中文：

- JVM 差分测试

白话解释：

- 把同一份样例放到不同 JVM 版本上运行
- 如果结果不一致，就说明这个样例可能触发了版本差异甚至潜在漏洞

### `manifest`

白话解释：

- 清单文件
- 这里的 `mutation_manifest.jsonl` 就是记录每个样例来源、算子、结果的结构化日志

### `JSONL`

白话解释：

- 每行一个 JSON 对象的文本格式
- 适合持续追加记录

### `repair`

中文：

- 修复

白话解释：

- 对大模型生成的代码做轻量纠偏
- 例如自动给越界的 `byte` 字面量加 `(byte)` 强转

### `filter`

中文：

- 过滤

白话解释：

- 把明显低质量、低价值或会放大噪声的样例直接丢掉

### `boundary`

中文：

- 边界条件

白话解释：

- 一些最容易出问题的极端值或特殊情况
- 例如 `0`、`-1`、`255`、`null`、空数组、越界长度

---

## 1. 目前项目的真实定位

当前项目并不是“调用几个大模型自动写代码”的简单流水线，而是一套**面向 Java API 的定向样例生成与反馈优化系统**。

目标不是单纯生成能编译的 Java 代码，而是：

1. 以某个目标 API 为中心。
2. 从 API 文档中学习其合法行为、边界行为和潜在风险点。
3. 生成围绕该 API 的测试代码。
4. 用编译器和后续运行日志反推生成错误模式。
5. 把错误模式再反馈给提示词生成器，持续优化后续样例。
6. 在此基础上加入**分类感知变异**，把“普通样例”推进成“更容易触发 JVM 版本差异的高价值样例”。

因此，这个项目的最终方向不是“文本生成”，而是：

**面向 JVM 差分测试的定向程序样例生成、变异与筛选框架。**

---

## 2. 当前工程中的完整工作流

下面按实际执行顺序把整条链路梳理一遍。

### 2.1 输入资产

系统依赖 4 类输入：

1. API 文档输入
   位置：
   - `config/documentation/*.md`

   作用：
   - 提供目标 API 的官方/半结构化说明
   - 包括构造器、方法、字段、异常、边界说明

2. 目标配置输入
   位置：
   - `config/targeted/*.yaml`

   作用：
   - 定义当前要 fuzz 的目标 API
   - 提供 prompt 触发语、输入前缀、批大小、模型配置、反馈参数

3. API 分类知识输入
   位置：
   - `api_classify.doc`
   - `五维全息分类漏斗 .doc`

   作用：
   - 提供 API 分类规则的外部知识来源

4. 分类结果缓存输入
   位置：
   - `outputs/api_classification/java_api_5d_classification.json`
   - `outputs/api_classification/java_api_5d_classification.csv`

   作用：
   - 作为 mutation profile 的上游输入

---

### 2.2 配置装配阶段

代码入口：

- `Fuzz4All/fuzz.py`
- `Fuzz4All/make_target.py`

流程：

1. `fuzz.py` 接收 `--config`。
2. `load_config_file()` 读取 YAML。
3. `make_target_with_config()` 把配置装配成 target 对象。
4. 对 Java 目标，创建 `JAVATarget`。

这一层的职责是：

- 不做复杂逻辑
- 只把配置传给真正执行链路

---

### 2.3 Prompt 初始构建阶段

核心文件：

- `Fuzz4All/target/target.py`
- `Fuzz4All/target/JAVA/JAVA.py`
- `Fuzz4All/util/api_request.py`

#### 2.3.1 基础 prompt 材料来自哪里

`Target._create_prompt_from_config()` 会从 `config/targeted/*.yaml` 里读取：

- `path_documentation`
- `path_example_code`
- `trigger_to_generate_input`
- `input_hint`
- `path_hand_written_prompt`
- `target_string`

拼成一个内部结构：

- `docstring`
- `example_code`
- `separator`
- `begin`
- `hw_prompt`
- `target_api`

这一步的本质，是把“YAML 配置”转成“模型可用的 prompt 配方”。

#### 2.3.2 DeepSeek 在哪里参与

DeepSeek 当前主要扮演 **auto-prompting prompt optimizer** 的角色。

执行位置：

- `Target.auto_prompt()`
- `Target._request_auto_prompt_candidates()`
- `request_engine()`

工作逻辑：

1. 把 API 文档、示例、目标 API 信息组成消息。
2. 请求 DeepSeek 生成：
   - 1 个 greedy prompt
   - N 个 candidate prompt
3. 对这些 prompt 做小批量验证。
4. 选择得分最好的 prompt 作为初始 prompt。

#### 2.3.3 为什么要先让 DeepSeek 生成 prompt，而不是直接让 Qwen 看全文档

原因有四个：

1. API 文档通常冗长，直接喂给代码模型，噪声大。
2. 代码模型更适合接收“压缩后的行为摘要”，而不是大段文档。
3. prompt 本身也需要竞争和筛选。
4. DeepSeek 在这里承担的是“文档蒸馏器”，不是最终代码生成器。

所以第一层实际上是：

**文档 -> 结构化简化 prompt**

而不是：

**文档 -> 直接代码**

---

### 2.4 Qwen 代码生成阶段

核心文件：

- `Fuzz4All/model.py`
- `Fuzz4All/target/target.py`

执行流程：

1. `Target.initialize()` 载入代码生成模型。
2. `Target.generate_model()` 用当前 prompt 请求 Qwen。
3. `Target.generate()` 把输出清洗成候选 Java 程序。

这里 Qwen 的职责是：

- 根据已经被 DeepSeek 压缩过的 prompt
- 输出一批 Java 代码种子

注意：

在现在的设计里，Qwen 负责的是**基础语义骨架生成**，不是最后的全部样例来源。

---

### 2.5 代码预处理与轻修复阶段

这是当前为了提高质量新增的优化层。

核心文件：

- `Fuzz4All/target/target.py`

主要函数：

- `_prepare_generated_code()`
- `_repair_java_code()`
- `_rewrite_numeric_byte_literals()`
- `_ensure_java_import()`
- `_is_viable_generated_code()`

这一层的意义非常重要：

大模型输出里常见一批“低价值噪声”：

- `byte[]` 中直接写 `255` / `128`
- `byte x = 255`
- 用 `Arrays.` 却不导入
- 调用虚构 API，如 `getBufferSize()`、`getBufSize()`、`getBuffer()`
- 把 `write(...)` 当成有返回值的表达式
- 花括号不配平

如果不在这里先处理，后续 mutation 只会把这些错误放大。

所以我们在生成后做两类操作：

1. **repair**
   - 自动把超界 `byte` 字面量改成 `(byte) 255`
   - 自动补 `import java.util.Arrays;`

2. **filter**
   - 直接丢弃明显不靠谱的 seed

这一步的设计思想是：

**让大模型负责生成结构，让规则层负责纠偏低级错误。**

---

### 2.6 编译与验证阶段

核心文件：

- `Fuzz4All/target/JAVA/JAVA.py`

主要函数：

- `validate_individual()`
- `determine_file_name()`
- `extract_javac_error_categories()`
- `map_error_categories_to_rules()`

执行逻辑：

1. 把生成的 Java 代码写到临时文件。
2. 用 `javac` 编译。
3. 根据返回码判断：
   - `SAFE`
   - `FAILURE`
   - `ERROR`
   - `TIMED_OUT`
4. 解析编译日志，把错误映射成结构化类别。

这一步本质上是把“原始编译器报错”转化成“机器可理解的反馈规则”。

---

### 2.7 运行反馈驱动的 prompt 更新阶段

核心文件：

- `Fuzz4All/target/target.py`

主要函数：

- `update()`
- `build_prompt_refresh_request()`
- `refresh_prompt_from_feedback()`
- `_create_feedback_prompt()`

执行逻辑：

1. 从本轮样例里提取 SAFE 示例。
2. 从本轮失败样例里提取失败规则。
3. 合并成运行时反馈 prompt。
4. 周期性触发 DeepSeek 做 runtime prompt refresh。

这一层的意义是：

- 模型不再每轮都从零开始
- 而是逐渐学会：
  - 哪些写法常失败
  - 哪些结构更容易成功

到这里为止，系统已经具备：

**文档学习 -> 提示词生成 -> 代码生成 -> 编译反馈 -> 提示词优化**

的闭环。

---

## 3. 为什么还必须加入 mutation

如果只有上面的闭环，系统主要会朝一个方向优化：

**更容易编译成功**

但 JVM 差分测试想要的是：

**更容易制造跨版本行为差异**

这两者不完全等价。

### 3.1 没有 mutation 时的典型问题

1. 生成样例容易“语义普通化”
   - 会调用常规 API
   - 但不够靠近边界

2. 即使能编译成功，也未必能覆盖足够极端的状态序列

3. 代码模型不擅长稳定地系统覆盖所有边界组合
   - 例如 `off/len` 组合边界
   - wrapper chain 边界
   - close/flush/write 顺序边界

因此 mutation 的定位不是“额外再做一点随机扰动”，而是：

**把大模型产出的普通种子，系统性推进到更靠近 JVM 差分测试的高价值区域。**

---

## 4. 为什么要先分类，再 mutation

mutation 不能是统一模板，因为不同 API 的风险结构完全不同。

### 4.1 例子

`BufferedOutputStream` 的关键风险点是：

- buffer 尺寸
- write 的 off/len
- flush/close 顺序
- 嵌套 wrapper

`Thread` 的关键风险点是：

- 线程状态切换
- 并发顺序
- 共享状态

`File` 的关键风险点是：

- 路径合法性
- 文件状态切换
- 权限和存在性

如果不分类，mutation 很容易变成：

- 对所有 API 都做同一套扰动
- 结果既低效又不专业

所以必须先做：

**API 风险画像**

再决定：

**应该投什么 mutation 算子**

---

## 5. 五维全息分类漏斗与 api_classify 的具体含义

当前分类脚本：

- `scripts/classify_java_apis.py`

输出：

- `outputs/api_classification/java_api_5d_classification.json`
- `outputs/api_classification/java_api_5d_classification.csv`
- `outputs/api_classification/java_api_5d_summary.md`

### 5.1 为什么叫“五维”

当前实现里使用了以下维度：

1. `package`
2. `type`
3. `interface`
4. `field`
5. `method`
6. `boundary`

严格说实现已经是“5 维主体 + 1 个边界补充维度”。

### 5.2 各维度的作用

#### `package`

看 API 所属包名，例如：

- `.io`
- `.net`
- `.security`
- `.concurrent`

原因：

包名通常最先暴露领域信息，是最稳定的粗粒度信号。

#### `type`

看类名/接口名，例如：

- `BufferedOutputStream`
- `Thread`
- `File`
- `SSLSession`

原因：

类名直接体现 API 主体语义，适合决定主标签。

#### `interface`

看实现的接口，例如：

- `Closeable`
- `Flushable`
- `Runnable`

原因：

接口通常说明对象的生命周期语义或交互模式。

#### `field`

看字段名，例如：

- `buf`
- `count`
- `position`
- `limit`

原因：

字段可以暴露该类的内部状态模型，尤其适合识别 buffer/resource 类型 API。

#### `method`

看方法名和方法签名，例如：

- `write(byte[], int, int)`
- `flush()`
- `close()`
- `connect()`

原因：

方法维度最直接决定后续 mutation 的操作槽位。

#### `boundary`

看文档中与异常、极值、空值、边界条件有关的文本。

例如：

- `null`
- `-1`
- `IOException`
- `IllegalArgumentException`

原因：

差分测试最关心的是边界和异常路径，因此必须把边界维度单独抽出来。

---

## 6. 当前分类标签的具体标准与原因

下面是当前主要标签及其设计原因。

### 6.1 `RESOURCE`

标准：

- API 与打开/关闭/flush/read/write 等资源行为强相关
- 常见于 `stream` / `reader` / `writer` / `session`

原因：

这类 API 的关键不只是参数值，而是**生命周期顺序**。

### 6.2 `BUFFER`

标准：

- 名称或字段明显体现缓冲区语义
- 常见字段：`buf`、`count`、`position`、`limit`
- 常见方法：`read(byte[])`、`write(byte[])`

原因：

这类 API 的关键风险是：

- 容量边界
- offset/length 组合
- 共享底层数组

### 6.3 `BATCH_OP`

标准：

- 方法签名中频繁出现数组 + `off/len`
- 常见于批量 read/write

原因：

这类 API 的差异点很多来自：

- 空长度
- 越界长度
- 偏移组合
- 大小切换

### 6.4 `FILE`

标准：

- 路径、文件系统、watch、resolve 等特征明显

原因：

文件类 API 的问题更偏：

- 路径状态
- 文件状态
- 环境差异

### 6.5 `CONCURRENT`

标准：

- `thread`、`lock`、`executor`、`future` 等特征明显

原因：

这类 API 的关键是：

- 调度顺序
- 竞争窗口
- 可见性

### 6.6 `SECURITY`

标准：

- `crypto` / `ssl` / `cert` / `permission` 等特征明显

原因：

这类 API 的主要差异来自：

- provider
- 算法选择
- 权限检查

### 6.7 `REFLECT`

标准：

- `Field` / `Method` / `invoke` / `setAccessible`

原因：

这类 API 容易制造强扰动，但噪声也大，默认不宜作为通用 mutation 主路径。

### 6.8 `CALLBACK`

标准：

- listener / event / handler / callback

原因：

这类 API 的差异主要来自注册顺序、重复注册、回调再入。

### 6.9 `TIME`

标准：

- instant / duration / date / zone / clock

原因：

这类 API 的关键边界是极值时间戳、时区和单位转换。

### 6.10 `NETWORK`

标准：

- socket / url / uri / http / bind / connect

原因：

关键在连接状态和超时路径。

### 6.11 `UTILITY` / `GENERIC`

标准：

- 泛工具类或无法强归类的类

原因：

作为兜底分类，保证所有 API 至少有基本 mutation 策略。

---

## 7. 分类结果如何转成 mutation profile

分类的终点不是“贴标签”，而是“转成变异配置”。

当前代码位置：

- `scripts/classify_java_apis.py`
- `Fuzz4All/mutation/profile_loader.py`

### 7.1 当前 profile 设计

#### `resource_buffer_batch`

适用：

- `primary_tag = RESOURCE`
- 且次标签中包含 `BUFFER` 或 `BATCH_OP`

典型目标：

- `BufferedOutputStream`
- `BufferedInputStream`

原因：

这一类 API 的最核心风险是：

- 资源生命周期
- buffer 大小
- 批量写入边界

#### `file_path_state`

适用：

- `primary_tag = FILE`

原因：

文件 API 不是主要围绕 `off/len`，而是围绕路径和状态切换。

#### `concurrent_sequence`

适用：

- `primary_tag = CONCURRENT`

原因：

并发类最重要的是顺序和共享状态，不是简单参数边界。

#### `generic_java`

适用：

- 其他暂未进入专属 profile 的 Java API

原因：

作为默认兜底路径，避免分类与 mutation 耦合过死。

### 7.2 profile 具体输出什么

每个 profile 至少输出：

- `mutation_profile`
- `priority_operators`
- `risk_points`

也就是说，分类结果要回答三个问题：

1. 这个 API 属于哪种风险结构。
2. 应优先使用哪些 mutation 算子。
3. 最需要撞击哪些边界。

---

## 8. 当前 mutation 设计的详细标准

### 8.1 mutation 插入在哪个环节

插入点：

- `Target.generate()`

执行顺序：

1. Qwen 生成基础 seed。
2. 对 seed 做轻修复与过滤。
3. 根据分类结果加载 mutation profile。
4. 对可用 seed 做 mutation。
5. 合并 seed 与 mutation 样例进入编译池。

这样做的原因是：

- 先让 LLM 负责结构
- 再让规则层负责精准边界推进

而不是反过来让 LLM 一次性覆盖所有细粒度边界。

### 8.2 为什么 mutation 放在生成后，而不是放在 prompt 里一次做完

原因：

1. prompt 中一次性塞太多边界，LLM 更容易乱。
2. mutation 需要可追踪、可归因。
3. mutation 更适合窄范围、强控制的程序变换。

---

## 9. `BufferedOutputStream` 的具体变异标准与原因

目标 API 当前分类：

- `primary_tag = RESOURCE`
- `secondary_tags = BATCH_OP, BUFFER`
- `mutation_profile = resource_buffer_batch`

### 9.1 `constructor_size_edge`

做什么：

- 改 `BufferedOutputStream` 构造器的 `size`

当前候选值：

- `1`
- `8`
- `8192`
- `0`
- `-1`

原因：

- size 直接影响内部缓冲行为
- 既有合法小值/默认附近值
- 也有非法值，可覆盖异常路径

### 9.2 `write_bulk_edge`

做什么：

- 改 `write(byte[], off, len)` 的 `off/len`

当前候选：

- `(0,0)`
- `(0,1)`
- `(1,1)`
- `(-1,1)`
- `(0,-1)`
- `(0,8)`

原因：

- 这是批量 API 的核心风险槽位
- 对差分测试有直接价值

### 9.3 `lifecycle_sequence`

做什么：

- 在 `write` 后插入：
  - `flush()`
  - `flush() + write(255)`
  - `close() + flush()`

原因：

- 资源类 API 的风险不只在参数，而在状态切换

### 9.4 `write_single_edge`

做什么：

- 改 `write(int)` 的单值

当前值：

- `0`
- `1`
- `127`
- `128`
- `255`
- `256`
- `-1`

原因：

- 这些值最容易触发边界语义

---

## 10. 为什么 mutation 不能对所有 seed 都做

这是当前系统里很关键的一条工程经验。

### 10.1 如果不筛 seed，会发生什么

当 seed 本身就有：

- 虚构 API
- 缺少必要 import
- 结构截断
- 明显错误的 byte 字面量

那么 mutation 会把这些错误批量扩散。

### 10.2 当前采取的策略

核心函数：

- `_is_viable_generated_code()`
- `_is_viable_mutation_seed()`
- `_repair_java_code()`

策略分两步：

1. 先 repair
   - 修 `byte` 边界值
   - 补 `Arrays` 导入

2. 再 filter
   - 过滤虚构 API
   - 过滤 `write(...) == ...`
   - 过滤花括号不平衡
   - 过滤明显幻觉写法

这样做的原因：

- repair 比直接丢弃更节省高质量 seed
- filter 则负责挡掉无法轻修复的坏 seed

---

## 11. mutation_manifest.jsonl 的作用

位置：

- 每个输出目录下的 `mutation_manifest.jsonl`

每条记录至少包含：

- `source_type`
- `operator`
- `parent_index`
- `target_api`
- `mutation_profile`
- `primary_tag`
- `all_tags`
- `file_name`
- `compile_result`
- `feedback_summary`
- `sanitized_message`

为什么它很重要：

1. 这是你工作量可视化的证据。
2. 它把“大模型输出”和“你自己做的 mutation 系统”区分开了。
3. 它支持后续实验分析：
   - 哪个算子最有效
   - 哪个 profile 最稳
   - 哪类失败最常见

---

## 12. 当前代码里各文件职责一览

### `Fuzz4All/fuzz.py`

职责：

- 主循环
- 写样例文件
- 调验证器
- 调 `update()`
- 现在也负责控制总样例数不越界

### `Fuzz4All/make_target.py`

职责：

- 配置组装
- 选择目标类型

### `Fuzz4All/target/target.py`

职责：

- prompt 生命周期
- 生成主流程
- mutation 接入
- manifest 记录
- 反馈聚合
- runtime prompt refresh

### `Fuzz4All/target/JAVA/JAVA.py`

职责：

- Java 编译验证
- Java 错误分类
- 结构化规则抽取

### `Fuzz4All/mutation/profile_loader.py`

职责：

- 读分类结果
- 分类标签映射为 mutation profile

### `Fuzz4All/mutation/java_mutator.py`

职责：

- Java 专属 mutation 算子实现
- 低预算下算子轮转

### `scripts/classify_java_apis.py`

职责：

- 对 `config/documentation` 的 API 文档做分类
- 输出 `mutation_profile / priority_operators / risk_points`

---

## 13. 这套方案的创新点怎么讲

### 创新点一

把 API 文档学习与程序变异连接起来。

不是“模型看文档写代码”这么简单，而是：

- 文档分类
- 风险画像
- profile 驱动变异

### 创新点二

提出“生成 + 变异 + 反馈再优化”的三段式闭环。

不是单靠 LLM 直接写最终样例，而是：

- LLM 生成骨架
- mutation 打边界
- 反馈再引导下一轮生成

### 创新点三

把 mutation 做成**分类感知**而不是统一随机扰动。

### 创新点四

引入 manifest，把每个样例的来源和算子都可追踪化。

---

## 14. 这项工作里你自己的工作量应该如何表达

你要明确强调：

1. 你不是只调用 DeepSeek/Qwen。
2. 你做的是一套完整的工程机制设计。

建议把自己的工作拆成 5 项：

1. 构建 API 文档处理与分类体系。
2. 设计分类到 mutation profile 的映射机制。
3. 实现 Java 专属 mutation 算子。
4. 实现编译反馈到 prompt 优化的闭环。
5. 构建 mutation manifest 与后续差分测试前置样例池。

这样老师就很难把你的项目理解成“全靠大模型自动完成”。

---

## 15. 当前已经落地的内容

当前仓库中已经实际落地的关键能力包括：

1. DeepSeek 自动生成和刷新 prompt。
2. Qwen 生成 Java 测试代码。
3. Java 编译反馈抽取为结构化规则。
4. API 分类结果输出到 JSON/CSV。
5. 分类结果转 mutation profile。
6. `BufferedOutputStream` 的分类感知 mutation。
7. mutation manifest 记录。
8. seed repair 与 bad seed filtering。

---

## 16. 还可以继续深入优化的方向

下面这些是下一阶段最值得继续做的。

### 16.1 更强的 Java seed repair

当前已修：

- `byte` 字面量边界
- `Arrays` 导入

还可继续加：

- `write(...) == ...` 自动改写
- 错误 helper 签名修复
- 常见虚构 API 黑名单替换

### 16.2 更强的 mutation 选择策略

现在是：

- profile 选算子
- 轮转 + 固定预算

后续可升级为：

- 根据近期 SAFE 率动态升降权
- 根据错误类别禁用某些算子

### 16.3 profile 扩展

当前优先覆盖：

- `resource_buffer_batch`
- `file_path_state`
- `concurrent_sequence`

后续可继续做：

- `security_provider_state`
- `reflect_access_path`
- `network_connection_state`

### 16.4 差分测试前置筛选层

当前主要是编译级筛选。

后续可加：

- 执行超时
- 输出一致性
- 运行状态摘要
- 多 JVM 版本运行分歧检测

---

## 17. 推荐的下一步实施顺序

如果继续深入优化，最稳的顺序是：

1. 继续增强 Java seed repair
2. 把 mutation operator 表现接入 prompt refresh
3. 为 `File`、`Thread` 等 profile 增加专属算子
4. 接入多版本 JVM 运行差分层
5. 做正式实验和对比组

---

## 18. 适合直接放进 PPT 的一句总述

可以直接这样概括你的方案：

**本工作构建了一套面向 Java API 的文档学习、提示词优化、代码生成、编译反馈闭环与分类感知变异协同框架，使大模型生成的基础样例能够进一步被定向推进到更适合 JVM 差分测试的高价值边界区域。**

---

## 19. 最终结论

你这套方案最有价值的地方，不是“大模型会写代码”，而是：

1. 你把 API 文档变成了可执行知识。
2. 你把编译器日志变成了结构化反馈。
3. 你把 API 分类变成了变异画像。
4. 你把生成与变异结合成了可持续优化的闭环。

也就是说，真正的核心不是模型本身，而是你搭建的这套：

**文档驱动 + 反馈驱动 + 分类感知 + 变异增强**

的工程化测试样例生产系统。

---

## 20. PPT 结构建议

下面给出一套适合课程汇报、阶段汇报、开题中期答辩都能用的 PPT 结构。

建议总页数：

- 10 到 14 页

如果时间较短，保留前 10 页即可。

### 第 1 页：题目页

建议标题：

- 面向 JVM 差分测试的分类感知生成与变异框架设计

副标题可选：

- 基于 API 文档学习、编译反馈优化与定制化 mutation 的 Java 测试样例生成系统

页面要点：

- 课题名称
- 你的姓名
- 指导老师
- 日期

### 第 2 页：研究背景与问题提出

页面要点：

- JVM 不同版本之间可能存在行为差异
- 差分测试需要大量高质量样例
- 传统随机 fuzzing 对复杂 Java API 不够友好
- 单靠大模型直接生成，代码往往“能看但不够尖锐”

这一页的目标：

- 说明为什么这个问题值得做

### 第 3 页：当前系统的总体目标

页面要点：

- 不是单纯生成能编译的代码
- 而是围绕目标 API 生成更适合 JVM 差分测试的高价值样例
- 系统目标分三层：
  - 文档学习
  - 反馈优化
  - 分类感知变异

### 第 4 页：原始流程总览

建议用流程图展示：

1. 读取 API 文档
2. DeepSeek 生成 prompt
3. Qwen 生成 Java 代码
4. 编译器验证并产生日志
5. DeepSeek 根据日志优化下一轮 prompt

这一页强调：

- 原系统已经有闭环
- 但还没有 mutation

### 第 5 页：原系统存在的问题

页面要点：

- 生成样例偏普通
- 边界覆盖不系统
- 失败样例中有大量低级噪声
- 难以高效触发 JVM 版本差异

建议结合你已有输出举例：

- 虚构 API
- 缺导入
- `byte` 边界错误
- 结构截断

### 第 6 页：改进思路总览

页面要点：

- 在原闭环基础上加入“分类感知 mutation 层”
- 形成：
  - 文档学习
  - prompt 优化
  - seed 生成
  - mutation 增强
  - 编译反馈
  - 再优化

这一页的目标：

- 把你的新增工作明确展示出来

### 第 7 页：API 分类体系设计

页面要点：

- 五维主体维度：
  - package
  - type
  - interface
  - field
  - method
- 边界补充维度：
  - boundary

再展示分类标签：

- RESOURCE
- BUFFER
- BATCH_OP
- FILE
- CONCURRENT
- SECURITY
- REFLECT
- CALLBACK
- TIME
- NETWORK
- GENERIC

### 第 8 页：为什么分类后再变异

页面要点：

- 不同 API 风险结构不同
- 统一 mutation 效果差
- 先分类，才能知道应该优先撞哪类边界

建议举例：

- `BufferedOutputStream` 关注 buffer 和生命周期
- `Thread` 关注并发顺序
- `File` 关注路径和状态

### 第 9 页：变异机制设计

页面要点：

- seed 不是全部都变异
- 先 repair 再 filter 再 mutation
- 典型算子：
  - `constructor_size_edge`
  - `write_bulk_edge`
  - `lifecycle_sequence`
  - `write_single_edge`

### 第 10 页：系统实现落点

页面要点：

- `target.py`
  - prompt 管理
  - mutation 接入
  - manifest 记录
- `JAVA.py`
  - 编译反馈分类
- `classify_java_apis.py`
  - 分类与 profile 输出
- `java_mutator.py`
  - Java 专属 mutation 算子

### 第 11 页：实验现象与阶段性结果

页面要点：

- mutation 已成功接入
- 样例总量已受控
- SAFE 比例提升
- 算子分布更加丰富

这一页最好放你最新输出目录中的统计数字。

### 第 12 页：创新点与个人工作量

页面要点：

- 文档学习与测试生成协同
- 分类感知 mutation
- 编译反馈驱动 prompt 优化
- 样例来源可追踪 manifest

同时强调你的个人工作：

- 分类体系设计
- mutation profile 设计
- Java 算子实现
- 反馈闭环改造

### 第 13 页：后续工作

页面要点：

- 继续增强 seed repair
- 扩展更多 profile
- 接入多 JVM 版本运行差分
- 构建正式实验对比组

### 第 14 页：总结页

一句话总结即可：

- 本工作构建了一套面向 Java API 的文档学习、反馈优化与分类感知变异协同框架，为后续 JVM 差分测试提供了更高价值的测试样例来源。

---

## 21. 逐页讲解稿

下面给出一版你现场基本可以直接讲的稿子。你可以照着念，也可以按你习惯改。

### 第 1 页讲稿：题目页

大家好，我这次汇报的题目是“面向 JVM 差分测试的分类感知生成与变异框架设计”。  
我的工作重点不是单纯调用大模型写代码，而是围绕 Java API 文档学习、编译反馈优化和分类感知变异，构建一套能够持续产生高价值测试样例的系统。

### 第 2 页讲稿：研究背景与问题提出

我这个工作的背景是，不同 JVM 版本在某些边界场景下可能存在行为差异，而差分测试正是通过同一份样例在不同版本上运行，去发现这种不一致。  
问题在于，差分测试非常依赖测试样例质量。传统随机 fuzzing 很难高效覆盖复杂 Java API，而如果只让大模型直接生成代码，生成结果往往比较普通，很多时候只是“看起来复杂”，但不一定真能触发高价值差异。

### 第 3 页讲稿：当前系统的总体目标

所以我的目标不是简单生成能编译的 Java 代码，而是围绕某个目标 API，生成更适合做 JVM 差分测试的测试样例。  
为此我把系统分成三层目标：第一层是文档学习，第二层是基于反馈的持续优化，第三层是在此基础上加入分类感知变异，让样例更接近真正的边界区域。

### 第 4 页讲稿：原始流程总览

我原来的系统已经实现了一条闭环流程。  
首先读取目标 API 对应的文档，然后让 DeepSeek 对文档进行学习和压缩，生成适合代码模型使用的 prompt。  
接着由 Qwen 根据 prompt 生成 Java 测试代码，再把这些代码交给编译器验证。  
验证后得到的日志会再次送回 DeepSeek，让它总结错误模式并优化下一轮 prompt。  
所以原系统已经具备“文档学习-代码生成-编译反馈-提示词优化”的闭环能力。

### 第 5 页讲稿：原系统存在的问题

但是原系统也有一个明显问题，就是生成出来的代码虽然能逐步变得更容易编译成功，但很多样例还是比较普通。  
另外失败样例中也会出现大量低级噪声，比如虚构 API、缺少导入、`byte` 边界值写错、结构截断等。  
这些错误虽然对 prompt 纠偏有帮助，但对后续 JVM 差分测试价值有限，因此必须继续往前推进。

### 第 6 页讲稿：改进思路总览

所以我在原闭环基础上又加入了一层“分类感知 mutation”。  
新的流程就变成：先从文档学习得到 prompt，再生成基础 seed，然后对 seed 做轻量修复和筛选，再结合 API 分类结果做定制化变异，最后把编译反馈继续回灌到下一轮。  
也就是说，大模型负责生成语义骨架，而 mutation 负责精准打击边界。

### 第 7 页讲稿：API 分类体系设计

为了让 mutation 不再是统一随机扰动，我先对 API 文档做了分类。  
分类时主要使用 package、type、interface、field、method 这五个主体维度，再加一个 boundary 边界维度。  
在此基础上把 API 分成 RESOURCE、BUFFER、BATCH_OP、FILE、CONCURRENT、SECURITY 等类别。  
这样分类的目的，不是单纯贴标签，而是建立 API 的风险画像。

### 第 8 页讲稿：为什么分类后再变异

原因在于，不同 API 的风险结构完全不同。  
比如 `BufferedOutputStream` 更关注 buffer 大小、off/len 边界和 flush/close 顺序；`Thread` 更关注并发顺序；`File` 更关注路径和状态切换。  
如果不分类，mutation 只能做成通用模板，效果会比较弱。  
而分类之后，就可以根据不同类型 API 定制不同的变异策略。

### 第 9 页讲稿：变异机制设计

在 mutation 这一层，我没有对所有 seed 都直接变异，而是先做 repair 和 filter。  
repair 负责修复一些大模型常见低级错误，比如越界的 `byte` 字面量和缺失的 `Arrays` 导入。  
filter 负责过滤明显低质量的样例，比如虚构 API、结构不完整、把 `write(...)` 当返回值使用等。  
在通过筛选后，再按 profile 投入具体算子，比如构造器 size 边界、批量 write 的 off/len 边界、生命周期顺序变异、单字节 write 边界等。

### 第 10 页讲稿：系统实现落点

在工程实现上，我把主要改动集中在几个文件里。  
`target.py` 负责 prompt 生命周期管理、mutation 接入、manifest 记录和反馈汇总。  
`JAVA.py` 负责 Java 编译验证和编译错误分类。  
`classify_java_apis.py` 负责把文档转成分类结果和 mutation profile。  
`java_mutator.py` 负责具体的 Java 变异算子实现。  
这样做的好处是每个模块职责比较清楚，后续扩展其他 profile 也比较方便。

### 第 11 页讲稿：实验现象与阶段性结果

从阶段性结果来看，mutation 已经成功接入，而且修复后样例总量已经能够受到配置约束，不会再无限膨胀。  
另外，manifest 中已经能够看到不同算子的实际分布，而不是只有单一变异动作。  
更重要的是，SAFE 比例相比早期版本已经明显提升，这说明 repair、filter 和分类感知 mutation 的组合是有效的。

### 第 12 页讲稿：创新点与个人工作量

我认为这个工作的创新点主要有四个。  
第一，把 API 文档学习和测试生成结合起来，不是直接让模型盲写代码。  
第二，引入了分类感知 mutation，而不是做统一随机扰动。  
第三，把编译反馈同时用于 prompt 优化和样例筛选。  
第四，通过 manifest 让每个样例的来源、算子和结果都可追踪。  
从个人工作量上看，我主要完成了分类体系设计、mutation profile 设计、Java 算子实现、反馈闭环改造和输出记录机制搭建。

### 第 13 页讲稿：后续工作

后续我会继续沿着三个方向深入。  
第一，增强 Java seed repair，进一步减少低级噪声。  
第二，扩展更多 API profile，比如文件、并发、安全等。  
第三，把当前编译级验证继续推进到多 JVM 版本运行级差分测试，真正验证样例在不同版本中的行为差异。

### 第 14 页讲稿：总结页

最后总结一下，我的工作构建了一套面向 Java API 的文档学习、反馈优化与分类感知变异协同框架。  
它的核心不是“大模型会写代码”，而是通过文档驱动、反馈驱动和分类感知变异，把大模型生成的基础样例推进成更适合 JVM 差分测试的高价值样例。  
我的后续工作会继续围绕变异质量提升和多 JVM 版本差分验证展开。

---

## 22. 现场讲解时的表达建议

### 22.1 建议多用这些表述

- 我设计了一套……
- 我把……转化成了……
- 我实现了……模块
- 大模型在我的系统中承担的是……角色
- 真正的核心改进在于……

### 22.2 尽量少用这些表述

- 模型自动帮我……
- 我直接让 AI 生成……
- 它自己会优化……

原因：

你要把系统主导权体现为“你在设计机制”，而不是“模型在替你完成工作”。

---

## 23. 如果时间不够，最短版汇报怎么讲

如果老师只给你 5 分钟，建议压缩成 6 页：

1. 背景与问题
2. 原始系统流程
3. 原始问题
4. 分类感知 mutation 改进
5. 阶段结果
6. 创新点与后续工作

对应的讲法就是：

- 为什么要做
- 原来怎么做
- 原来哪里不够
- 我新加了什么
- 现在效果怎么样
- 后面还要继续怎么做
