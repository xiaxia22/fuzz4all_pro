/* Construct the Duration object using valid and invalid arguments such as null, 0, -1, and very large values. Call the constructor with various time units like days, hours, minutes, and seconds. Wrap all constructors in try-catch blocks to handle potential exceptions. */
/* Please create a very short Java program that uses java.time.Duration in diverse and tricky ways. Use only standard JDK time APIs. Focus on zero, negative, very large, parsed, and arithmetic boundary durations, unit conversion edge cases, and documented public factory or instance methods, but avoid fabricated helpers or undefined temporal types. */
import java.time.Duration;