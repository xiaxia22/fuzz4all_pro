# Annotation Type Label

**Source:** `jdk.jfr\jdk\jfr\Label.html`

### Class Description

```java
@Target
({
TYPE
,
FIELD
,
METHOD
})

@Retention
(
RUNTIME
)
public @interface
Label
```

Annotation that sets a human-readable name for an element (for example,

"Maximum Throughput"

).

Use headline-style capitalization, capitalize the first and last words, and
all nouns, pronouns, adjectives, verbs and adverbs. Do not include ending
punctuation.

The label should not be used as an identifier, see

Name

.

**Since:** 9

---

### Field Details

*No entries found.*

### Constructor Details

*No entries found.*

### Method Details

#### value

String Returns a human-readable name for the annotated element.

---

### Additional Sections

#### Annotation Type Label

```java
@Target
({
TYPE
,
FIELD
,
METHOD
})

@Retention
(
RUNTIME
)
public @interface
Label
```

Annotation that sets a human-readable name for an element (for example,

"Maximum Throughput"

).

Use headline-style capitalization, capitalize the first and last words, and
all nouns, pronouns, adjectives, verbs and adverbs. Do not include ending
punctuation.

The label should not be used as an identifier, see

Name

.

**Since:** 9

@Target

({

TYPE

,

FIELD

,

METHOD

})

@Retention

(

RUNTIME

)
public @interface

Label

Annotation that sets a human-readable name for an element (for example,

"Maximum Throughput"

).

Use headline-style capitalization, capitalize the first and last words, and
all nouns, pronouns, adjectives, verbs and adverbs. Do not include ending
punctuation.

The label should not be used as an identifier, see

Name

.

Use headline-style capitalization, capitalize the first and last words, and
all nouns, pronouns, adjectives, verbs and adverbs. Do not include ending
punctuation.

The label should not be used as an identifier, see

Name

.

The label should not be used as an identifier, see

Name

.

=========== ANNOTATION TYPE REQUIRED MEMBER SUMMARY ===========

- Required Element Summary

Required Elements

Modifier and Type

Required Element

Description

String

value

Returns a human-readable name for the annotated element.

Required Element Summary

Required Elements

Modifier and Type

Required Element

Description

String

value

Returns a human-readable name for the annotated element.

---

#### Required Element Summary

Returns a human-readable name for the annotated element.

============ ANNOTATION TYPE MEMBER DETAIL ===========

- Element Detail

- value

```java
String
value
```

Returns a human-readable name for the annotated element.

**Returns:** a human-readable name, not

null

Element Detail

- value

```java
String
value
```

Returns a human-readable name for the annotated element.

**Returns:** a human-readable name, not

null

---

#### Element Detail

value

```java
String
value
```

Returns a human-readable name for the annotated element.

**Returns:** a human-readable name, not

null

---

#### value

String

value

Returns a human-readable name for the annotated element.

---

