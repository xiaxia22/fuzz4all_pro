现在这套系统的主架构已经打通了：config/documentation：每个 API 一份文档outputs/api_classification：离线 5D 分类结果config/targeted：每个 API 一份 target 配置Fuzz4All/mutation/profile_loader.py：分类结果映射到 primary_tag / mutation_profile / active_profilesFuzz4All/mutation/java_mutator.py：按分类执行通用变异因子Fuzz4All/target/JAVA/JAVA.py：javac/运行反馈归因Fuzz4All/target/target.py：autoprompt、生成、feedback、refresh 闭环这意味着“文档驱动 + 分类驱动变异 + 反馈优化”的主体不是问题，问题已经收敛到种子稳定性、弱类效果、个别波动类。目前已经确认没问题的部分分类命中是通的10 类代表 API 都能进入正确 primary_tag / mutation_profile分类变异是通的security_* / file_* / reflect_* / concurrent_* / callback_* / time_* / network_* / utility_* / jvm_mgmt_* / resource_* 都真正出现在 manifest 里反馈闭环是通的best_prompt.txtruntime_prompt_*.txtruntime_rules_*.txtrefresh_status_round_*.txtrefresh_decision_round_*.txt这些链路已经在工作强类已经基本可用SECURITYCONCURRENTNETWORKUTILITYTIMERESOURCE 基本可用现在真正存在的问题1. 弱类还没有完全拉起来主要是：CALLBACKJVM_MGMT现象：分类和 operator 都对但 SAFE 仍然偏低，甚至长期接近 0说明不是“分类没生效”，而是seed 质量和反馈约束还不够强根因：seed 仍会幻觉出不存在的方法/辅助类合法重载调用容易写错变量声明、参数顺序、类型一致性不稳2. 波动类不稳定主要是：FILEREFLECT现象：某些轮次能很好某些轮次又明显退化根因：prompt 候选质量有波动结构级过滤还不够强弱 seed 被 mutation 放大后容易失稳3. RESOURCE 虽然起来了，但还没完全发挥现象：现在已经出现 resource_* / buffer_*SAFE 也有了但仍不是最稳定的一类根因：resource 类算子虽然更通用了，但对更广的 InputStream/Reader/Writer/Closeable 还不够彻底有效样例还会被 seed 质量限制4. 反馈规则还不够“细颗粒”虽然已经比之前好很多，但还有缺口：CALLBACK 需要更强的 overload/signature 约束JVM_MGMT 需要更强的 static factory / MXBean query 约束REFLECT 需要更强的 receiver / helper type / accessible API 约束也就是说，现在不是没反馈，而是反馈还不够像“针对这类 API 的精细指导”。5. 仍缺一轮“最终可提交”的统一验证目前你有很多轮 targeted* 结果，但还没有一轮可以非常干脆地说：10 类代表 API 都进入正确 profile10 类都有分类 operator强类稳定弱类不再 0 SAFErefresh 证据完整可直接写进报告/论文/答辩材料当前还需要验证的东西CALLBACK是否能稳定摆脱 0 SAFE是否不再大量幻觉 callback helper/proxy APIJVM_MGMT是否能稳定进入标准 ManagementFactory.get*MXBean() 路径是否不再发明 bean/helper/query 方法REFLECT是否能稳定摆脱自造辅助类和 receiver/type 错误FILE是否能稳定保持较高 SAFE，不再大起大落RESOURCE是否能继续扩大 resource_* / buffer_* 的有效贡献最终 10 类统一回归代表集固定为：CipherFileMethodThreadPropertyChangeSupportDurationSocketURIManagementFactoryBufferedInputStream完整改动方案下面这套方案目标是：一次性把系统收口到“整体闭环完整、效果够好、后面只做小调参即可提交”的状态。第一部分：收口原则只做三类改动，不再大改架构：分类级规则增强按 primary_tag 强化 imports / filter / feedback / prompt 约束结构级垃圾过滤拦截明显非法 Java 语法、明显幻觉模式代表 API 验证增强只在 target trigger 层做少量补强不把核心逻辑写成单 API 特判第二部分：必须完成的代码改动A. Fuzz4All/target/target.py目标：稳定 import 自动修复保证 refresh 证据链完整要点：保留现在的 _ensure_java_import() 词边界匹配保留 refresh_status_round_* / refresh_decision_round_*不再改大逻辑状态：这块已经基本够了，只需要保持B. Fuzz4All/target/JAVA/JAVA.py目标：把错误归因做到足够细必须保留并继续扩的错误类别：nonexistent_methodinvalid_overload_callinvalid_constructor_callmissing_importincompatible_typesundeclared_variableinvalid_generic_syntaxprotected_member_access还要补的分类级反馈重点：CALLBACKdocumented add/remove/fire overloadlistener/event/helper object must be locally declaredJVM_MGMTstatic ManagementFactory.get*MXBean() / getPlatformMBeanServer()valid ObjectNameno invented helper/proxy/query methodsREFLECTdeclared receiver/helper type onlyvalid accessible API onlyFILElegal constructors onlyavoid ambiguous new File(null)这是最终系统质量最关键的一层。C. Fuzz4All/mutation/profile_loader.py目标：把弱类和波动类的 repair_rules.imports 与 filter_rules.reject_patterns 补全必须继续做的分类级增强：FILEimports: File / IOException / Path / Paths / Filesreject: \x..、new File(null,...)、必要时补 new File(null) 歧义模式REFLECTimports: Method / Field / Constructor / Map / List / Arrays / HashMapreject: 自造 annotation、非法 default method、非法 generic syntax、明显伪 helper type 模式CALLBACKimports: PropertyChangeEvent / PropertyChangeListener / PropertyChangeSupportreject: indexed helper、proxy-helper 幻觉、java.nio.serialize(...) 这类明显伪调用模式JVM_MGMTimports: ManagementFactory / MXBean interfaces / ObjectNamereject: MBeanProxy、明显非 JDK 管理 helper/proxy 名称模式CONCURRENTreject: 死循环、极端 sleep这部分是“通用类级约束”的主战场，不算写死。D. Fuzz4All/mutation/java_mutator.py目标：保持分类级 operator，不再退回 generic强化弱类的“可编译友好性”重点：RESOURCE继续往 InputStream/Reader/Writer/Closeable 泛化CALLBACKmutation 尽量围绕合法 listener registration / fire overload 做轻量扰动JVM_MGMTmutation 聚焦标准 bean/query/object-name 路径，不做大幅发散E. config/targeted/*.yaml目标：维持“分类级通用模板 + 代表 API 验证增强”原则：批量 target 继续用分类模板生成10 个代表 API 可以保留稍强 trigger但不把逻辑写死进代码第三部分：验证方案第一步：弱类专项验证只跑 4 个：PropertyChangeSupportManagementFactoryMethodFile达标标准：PropertyChangeSupport：SAFE > 0 且持续增长ManagementFactory：摆脱 SAFE = 0Method：恢复到可用区间File：不再大幅波动第二步：10 类统一回归固定跑这 10 个代表 API。统一检查：primary_tagmutation_profileoperator 分布SAFE / FAILURE / ERRORruntime_rules_1.txtrefresh_status_round_*refresh_decision_round_*第三步：形成提交版结论只要满足下面条件，就可以视为“可提交版”：10 类全部命中正确分类和 profile10 类全部出现对应分类 operator强类稳定可用：SECURITY / CONCURRENT / NETWORK / UTILITY / TIME / RESOURCE波动类稳定下来：FILE / REFLECT弱类摆脱纯失败：CALLBACK / JVM_MGMTrefresh 闭环有可观察证据最终系统应该达到的效果最终不是“偶尔能跑几个 API”，而是：API 文档进入系统离线分类输出主标签和复合画像运行时按分类装载 profile 和 operatorDeepSeek 根据文档生成 promptQwen/Ollama 生成 seed分类变异因子生成 mutationjavac/运行反馈被结构化归因DeepSeek 根据规则和 SAFE 样例更新 prompt下一轮继续迭代10 类代表 API 都能给出可解释、可复现、可写报告的结果这就是“整体闭环完成”。